# AWS IoT Core エンドポイント (必須)
# 確認コマンド: aws iot describe-endpoint --endpoint-type iot:Data-ATS --query endpointAddress --output text
IOT_ENDPOINT=xxxxxxxxxx.iot.ap-northeast-1.amazonaws.com

# AWS リージョン
AWS_DEFAULT_REGION=ap-northeast-1

# テレメトリ送信間隔 (秒)
TELEMETRY_INTERVAL=1.0
